{
  "name": "cipher-health",
  "private": true,
  "scripts": {
    "up": "docker-compose -f docker/docker-compose.yml up --build -d",
    "down": "docker-compose -f docker/docker-compose.yml down",
    "predev": "pnpm run up",
    "dev": "FORCE_COLOR=true turbo dev",
    "clean": "FORCE_COLOR=true turbo clean",
    "fclean": "FORCE_COLOR=true turbo fclean && rm -rf node_modules .turbo pnpm-lock.yaml",
    "build": "FORCE_COLOR=true turbo build",
    "test": "FORCE_COLOR=true turbo test",
    "check-types": "FORCE_COLOR=true turbo check-types",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@turbo/gen": "1.11.3",
    "turbo": "1.11.3",
    "husky": "8.0.3"
  }
}
