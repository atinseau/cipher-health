{
  "name": "cipher-health",
  "private": true,
  "scripts": {
    "up": "docker-compose -f docker/docker-compose.yml up --build -d",
    "down": "docker-compose -f docker/docker-compose.yml down",
    "dev": "FORCE_COLOR=true turbo dev --filter=!@cipher-health/frontend",
    "build": "turbo build",
    "turbo": "turbo"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.16",
    "turbo": "^1.10.16"
  }
}