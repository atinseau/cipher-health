apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nextjs-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: {{ .Values.frontend.ingress.host }}
      http:
        paths:
          - pathType: Prefix
            backend:
              service:
                name: nextjs-service
                port:
                  number: 80
            path: /
