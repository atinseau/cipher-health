{
  "name": "cipher-health-api",
  "version": "0.0.1",
  "main": "build/index.js",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "start": "node build/index.js",
    "dev": "chokidar src -c 'node esbuild.mjs; node build/index.js' --initial --silent",
    "build": "NODE_ENV=production node esbuild.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.19.5",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "chokidar-cli": "^3.0.0",
    "fastify": "^4.24.3",
    "fastify-cli": "^5.9.0"
  }
}